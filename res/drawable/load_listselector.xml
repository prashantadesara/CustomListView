<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android" android:visible="true">

    <!-- the list items are disabled -->
   <item android:drawable="@drawable/shape_row" android:state_window_focused="false"/>

    <!-- the list items are enabled, in focus but not being touched/pressed (idle) -->
    <item android:drawable="@drawable/shape_row" android:state_focused="false" android:state_pressed="false" android:state_selected="false" android:state_window_focused="true"/>

    <!-- the catch-all -->
<!--     <item android:drawable="@android:color/transparent"/> -->
    <item android:drawable="@drawable/shape_row_on"/>

</selector>